# Galaxy backend

Express + Mongoose backend for the Galaxy app.

Setup

1. Copy `.env.example` to `.env` and edit `MONGO_URI` and `JWT_SECRET`.
2. Install dependencies:

```bash
cd new/backend
npm install
```

3. Start the server:

```bash
npm run dev
```

The server listens on port `3001` by default.
